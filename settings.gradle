pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
        // added local library directory
        flatDir {
            dirs 'libs'
        }
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "HelloKebbi"
include ':app'
